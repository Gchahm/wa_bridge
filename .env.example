# Database (Supabase)
DATABASE_URL=postgresql://wa_bridge_app:your-password@db.your-project-ref.supabase.co:5432/postgres

# Optional: webhook URLs for forwarding messages to external services
# MESSAGE_WEBHOOK_URL=https://your-n8n.example.com/webhook/whatsapp
# VOICE_WEBHOOK_URL=https://your-n8n.example.com/webhook/whatsapp-audio

# Optional: Supabase Storage for media files (images, videos, audio, documents)
# Both must be set to enable media storage. Without these, the bridge still works but skips media download.
# SUPABASE_URL=https://your-project-ref.supabase.co
# SUPABASE_SERVICE_KEY=your-service-role-key
